version: "3.8"
services:
  mysql-database:
    build: ./mysql-database
    environment:
      MYSQL_ROOT_PASSWORD: Mp4135*6
      MYSQL_DATABASE: itesm502
    restart: always
  web-nodejs:
    build: ./rick-morty-express-handlebars-main
    environment:
      MYSQL_DATABASE: itesm502
      MYSQL_USER: root
      MYSQL_PASSWORD: Mp4135*6
      MYSQL_HOST: mysql-database
    ports:
      - "3000:3000"
    depends_on:
      - mysql-database
    restart: on-failure
