module.exports = {
    database: {
        host: process.env.MYSQL_HOST || "localhost",
        user: process.env.MYSQL_USER || "root",
        password: process.env.MYSQL_PASSWORD || "Mp4135*6",
        database: process.env.MYSQL_DATABASE || "itesm502"
    }
}